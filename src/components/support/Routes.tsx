/* Import Dependencies */
import { Route } from "react-router-dom";

/* Import Components */
import Support from "./Support";

/* Routes associated with the support page */
const routes = [
    <Route key="support" path="/support" element={<Support />} />
];

export default routes;